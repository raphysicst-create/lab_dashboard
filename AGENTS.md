# 이 프로젝트에서 작업할 때

먼저 `README.md`와 `WORKSPACE_STATUS.md`를 읽어 현재 사용자 결정 사항과 미완료 작업을 확인한다. 최초 기획 문서는 `docs/initial-plan.md`이며 이후 사용자 지시가 우선한다.

- 최우선 원칙: 실험 및 약품의 미확인 내용을 임의로 작성하지 않는다. 데이터는 `null`, 제품 표시가 필요하면 `미확인`을 사용한다.
- 원자료와 준비물 원문을 보존한다. 원자료의 규격·농도·온도로 필요 수량을 추정하지 않는다.
- 사용자 지정 학년 매핑은 1학년 1~8단원, 2학년 9~15단원이다.
- 사용자 요청으로 제거한 준비 목록·검색 범위·상세 메타데이터·약품 근거 표시를 다시 도입하지 않는다.
- 교과서 대조 상태와 내부 검토 기록은 웹 제품 화면에 노출하지 않는다. GitHub에는 전체 작업 백업 요청에 따라 보존한다.
- 기존 `outputs/review/science_experiment_review.xlsx`를 임의로 재생성하거나 덮어쓰지 않는다.
- 웹사이트 코드·공개 데이터는 `site/dist`에만 둔다. 전체 저장소의 실제 GitHub Pages 워크플로는 루트 `.github/workflows/deploy-pages.yml`이다.
- 기술 스택은 HTML, CSS, JavaScript, JSON을 유지한다. 검증·변환 도구는 `scripts/README.md`를 참고한다.
- `output/validation/`의 배포 상태는 과거 확인 기록이다. 게시 성공 여부를 보고하기 전에 최신 실행과 공개 화면을 확인한다.
